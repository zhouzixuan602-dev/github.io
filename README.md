# Neural Need Web (Local-Only)

A tiny open-source, GitHub Pages-ready web app:
- Create "Need" posts
- Comment thread (text-only)
- Interest toggle
- Offline storage via localStorage (no backend)

## Run locally
Just open `index.html` in a browser.

## Deploy
Push to GitHub and enable GitHub Pages (main branch / root).
